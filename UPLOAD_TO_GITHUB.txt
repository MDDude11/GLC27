GLC27 ITB v19.0.0 — Match experience + recursive Super Overs

Copy the CONTENTS of this folder into the repository root, not the containing folder itself.
The repository root must directly contain .github, public, src, package.json, package-lock.json, vite.config.js and the HTML entry files.

VERIFY REPOSITORY BEFORE GIT COMMANDS
cd /d "C:\Users\manis\Downloads\GLC27_v11_FIXED\GLC27_v11_FIXED"
git rev-parse --show-toplevel
git status

EXPECTED REPOSITORY ROOT
C:/Users/manis/Downloads/GLC27_v11_FIXED/GLC27_v11_FIXED

COMMIT + PUSH
git add -A
git commit -m "Release ITB v19 recursive Super Over and match UX"
git push origin main
git log -2 --oneline

GitHub Actions will run:
  npm ci
  npm run lint
  npm run build
  npm run verify-build

Do not run Git commands from the downloaded/source-folder path unless git rev-parse --show-toplevel confirms the repository root above.

Verification focus:
  - production build completes and verify-build passes
  - service-worker cache is glc27-v19-shell-v1
  - scorer.html remains the stable scorer entry point
  - tied stages expose recursive BEGIN SUPER OVER controls
  - main match + all Super Overs appear in viewer progression and scorecard
