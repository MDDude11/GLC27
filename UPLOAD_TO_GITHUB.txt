GLC27 / ITB13 — Internal Test Build

This package excludes the .git directory so it cannot overwrite your current repository history.
Extract it, replace the project files in your existing local Git repository, keep that repository's .git folder, then run git add ., git commit, and git push.
